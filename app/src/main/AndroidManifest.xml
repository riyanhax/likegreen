<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pywl.likegreen">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.PersonalDataActivity" />
        <activity android:name=".activity.GuanggaoAndLiveActivity" />
        <activity android:name=".activity.AuditAndLiveActivity" />
        <activity android:name=".activity.MyMessageAndDirectActivity" />
        <activity android:name=".activity.SystemSettingsActivity" />
        <activity android:name=".activity.PlayBackActivity" />
        <activity android:name=".activity.ShareLiftActivity"></activity>
    </application>

</manifest>